// cache le formulaire par défaut et le mets en display block à l'couteur d'évenement "click" sur l'image de fleur
    // document.addEventListener("DOMContentLoaded", function() {
    //     let profileIcon = document.getElementById("profileIcon");  // récupère les id et les stocks dans les variable

    //     profileIcon.addEventListener("click", function() { // écouteur d'évenemnt "click" sur l'image de fleur stocké dans la variable profileIcon
    //         event.preventDefault();
    //         if (profileForm.style.display === "none") { // condition si est strictement egal à none
    //             wrapper.style.display = "block";
    //             profileForm.style.display = "block"; // alors devient block au click
    //         } else {
    //             wrapper.style.display = "none";
    //             profileForm.style.display = "none"; // sinon reste en display none
    //         }
    //     });
    // });